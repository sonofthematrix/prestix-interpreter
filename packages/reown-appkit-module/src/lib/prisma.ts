/**
 * Stub for package build only.
 * When the package is type-checked, root code that imports @/lib/prisma may be pulled in;
 * this stub satisfies the module resolution. The actual prisma client lives in the workspace root.
 */
export const prisma: unknown = {} as any;
